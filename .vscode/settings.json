{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "timetracker",
            "database": "${workspaceFolder:timetracker}/timetracker.db"
        }
    ]
}